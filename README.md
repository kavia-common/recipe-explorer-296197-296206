# recipe-explorer-296197-296206

This project’s frontend runs fully static (no runtime envs, no network calls) using in-memory mock data. See `recipe_explorer_frontend/README.md` for details and how to re-enable API mode if needed.